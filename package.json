{
  "type": "module",
  "name": "hadfieldnoblinwebsite",
  "version": "1.0.0",
  "description": "Website for Hadfield + Noblin Architects.",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "node --test test/**/*.test.js",
    "start": "fastify start -l info app.js",
    "dev": "fastify start -w -l info -P -a 0.0.0.0 app.js"
  },
  "keywords": [],
  "dependencies": {
    "fastify": "^5.0.0",
    "fastify-plugin": "^5.0.0",
    "@fastify/autoload": "^6.0.0",
    "@fastify/sensible": "^6.0.0",
    "@fastify/static": "^10.0.0",
    "fastify-cli": "^8.0.0"
  },
  "devDependencies": {
    "c8": "^11.0.0"
  }
}